<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.SbillitOrderMapper">

    <select id="findAllOrders" resultType="SbillitOrder">
        select * from `sbillit_order` where 1=1
    </select>
    
    <select id="findOrderbyId" parameterType="int" resultType="SbillitOrder">
        select * from `sbillit_order` where id=#{value}
    </select>

</mapper>